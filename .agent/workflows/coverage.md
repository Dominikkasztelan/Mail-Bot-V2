---
description: run pytest with coverage report showing missing lines
---

// turbo
1. Run `pytest --cov=. --cov-report=term-missing` from the project root to execute all tests and display code coverage with uncovered line numbers.
