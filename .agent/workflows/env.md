---
description: check Python version and key environment information
---

// turbo
1. Run `python --version` to show the current Python version.
// turbo
2. Run `pip --version` to show pip version and environment path.
// turbo
3. Run `python -c "import sys; print(sys.executable)"` to show the active Python interpreter path.
