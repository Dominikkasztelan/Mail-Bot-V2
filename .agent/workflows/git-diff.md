---
description: show unstaged and staged changes before committing
---

// turbo
1. Run `git diff` to show all unstaged changes in the working directory.
// turbo
2. Run `git diff --staged` to show all staged changes ready to commit.
