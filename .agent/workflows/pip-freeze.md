---
description: update requirements.txt with currently installed packages
---

1. Run `pip freeze > requirements.txt` to overwrite requirements.txt with all currently installed packages and their exact versions.
