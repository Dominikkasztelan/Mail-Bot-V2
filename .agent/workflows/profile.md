---
description: profile a Python script for performance analysis
---

1. Run `python -m cProfile -s cumulative <script_path>` replacing `<script_path>` with the target Python file to display a sorted performance report.
